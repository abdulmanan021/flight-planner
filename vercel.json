{
    "builds": [
      {
        "src": "/app.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/frontend/src/$1" },
      { "src": "/api/(.*)", "dest": "backend/app.js" }
    ]
  }
  